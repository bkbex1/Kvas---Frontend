<div class="container narrow page-space center"><h1>{{ shop.name | escape }}</h1><div class="rte">{{ shop.password_message }}</div>{% form 'storefront_password', class: 'comment-form' %}{{ form.errors | default_errors }}<label for="StorePassword">{{ 'general.password' | t }}</label><input id="StorePassword" type="password" name="password" autocomplete="current-password"><button type="submit" class="button">{{ 'general.enter' | t }}</button>{% endform %}</div>
{% schema %}
{
  "name": "Защитен магазин",
  "tag": "section",
  "class": "kv-section",
  "settings": [],
  "presets": [
    {
      "name": "Защитен магазин"
    }
  ]
}
{% endschema %}
